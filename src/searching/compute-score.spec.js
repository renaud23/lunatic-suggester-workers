import compute from './compute-score';

const e1 = { id: 1, text: 'toto tata titi' };
const e2 = { id: 2, text: 'toto   tutu' };

describe('', function () {
  it('', function () {
    expect(true).toBe(true);
  });
});
